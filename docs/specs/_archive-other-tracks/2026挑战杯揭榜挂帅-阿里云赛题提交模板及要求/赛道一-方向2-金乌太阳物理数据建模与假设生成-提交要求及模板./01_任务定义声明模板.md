# 任务定义声明模板（必填）

队名：____________________
作品名称：____________________
联系人 / 邮箱 / 电话：____________________
提交日期：____________________
路线（可多选）：□ 训练模型　□ 智能体

---

## 1. 输入

- 使用模态（可多选）：□ PNG Th1000　□ FITS　□ 标签 CSV 中的磁特征列　□ movies　□ 其它：________
- 单样本定义（例如：单帧磁图 / 时间序列长度 N / 是否拼接特征）：


- 预处理摘要（归一化、裁剪、增强等）：


---

## 2. 预报任务（不限制二分类/多分类，不限制时间窗）

### 2.1 时间窗

- 主报告时间窗（可多个）：________（单位：小时；须与标签或自定义映射一致）
- 自定义时间窗说明（若不用公开 CSV 标准列，写清如何从事件表生成标签）：


### 2.2 类别体系（择一为主，可附加）

**方案 A：二分类**

- 正类定义（例：≥M1.0、≥M5.0、≥X1.0）：________
- 对应公开标签列名（若适用，例：`flare_label_M1.0_24hr`）：________
- 负类定义：________

**方案 B：多分类**

- 类别列表（例：0 / C / M / X，或自定义）：________
- 标签来源（例：`flare_class_24hr` 映射规则）：


- **折叠为业务二分类的规则**（私有终评横向比较必填，例：将 M、X 合并为正类，其余为负类）：


**方案 C：其它**（回归等级、多标签等）


- 与组委会二分类指标（TSS/HSS）的对齐方式：


### 2.3 输出

- 输出形式：□ 类别　□ 概率　□ 等级分数　□ 其它：________
- 若为概率：阈值策略（固定 0.5 / 在 val 上最大化 TSS 等）：________
- 最终提交是否包含概率列：□ 是　□ 否

---

## 3. 数据使用

- □ 仅使用竞赛公开的 JW-FD 子集
- □ 额外使用外部数据 / 预训练（须列出）：

| 名称 | 用途 | 获取方式 | 许可 |
|------|------|----------|------|
|      |      |          |      |

- 训练仅用官方 `train`，调参仅用 `val`：□ 确认
- 未使用公开 `test` 标签进行训练或调参：□ 确认

---

## 4. 复现入口（与代码包一致）

- 推理命令（须可直接复制）：

```bash
# 示例，请改成队伍实际命令
python infer.py --data_root /path/to/data --split test --out pred.csv
```

- 权重路径或镜像名称：________
- 权重 SHA256（或说明见复现包 `checksums.txt`）：________
- 预计推理耗时 / 硬件：________

---

## 5. 诚信声明

本队确认：接受组委会在公开数据上的复现抽查，以及在**私有评测数据集**上的终评；公开 test 成绩不作最终排名唯一依据。虚假申报、无法复现或私有集异常表现，接受成绩取消或降级。

队长签字（或电子确认）：____________________　日期：________
