{
  "schema_version": 1,
  "experiment": "science125-memory-ablation",
  "status": "pre_registered_protocol",
  "registered_at": "2026-08-13",
  "registered_commit": "fe417584605716f71ef4e101c2ea4b55cbee19cf",
  "question": "跨 run 战役记忆（memory/）对 Science-125 单题交付率有没有可观测的方向性影响？关掉它，同一批题的交付会变差、持平，还是变好？",
  "estimand": "Architecture-path trade-off under one fixed serial batch order; not a pure memory causal effect, for four reasons this protocol records before any run starts. (1) 顺序依赖：ON 臂的记忆随批次累积，第 120 题读到的 memory/ 与第 1 题不是同一个对象，因此 ON 臂内部 125 个 run 不独立，估计的是「这一条特定串行顺序下的记忆机制」，不是「记忆」本身。(2) 时间错位：Phase B 在 Phase A 之后跑，harness commit 钉死，但 arXiv 语料与外部 API 状态会漂移。(3) tool visibility differs：memory_search 工具在两臂都对 agent 可见，OFF 臂只是让它返回 enabled=false，所以两臂的工具面相同而工具效用不同——被消融的是数据不是接口，agent 仍可能把一次空调用花在它上面。(4) 单臂单次：每题每臂只跑一次，run 内随机性与臂间差异不可分离，本协议不做重复采样（见 statistics）。",
  "arms": {
    "phase_a": {
      "name": "memory_on_full",
      "memory_arm": "on",
      "questions": "1-125（全量）",
      "command": "uv run python -m app.batch --ids 1-125",
      "semantics": "跨 run 记忆读 + 写回全开；meta.memoryArm=on",
      "role": "主交付。M4/M5/M7/M8 与 7 类失败分布的全量证据来源。"
    },
    "phase_b": {
      "name": "memory_off_subset",
      "memory_arm": "off",
      "questions": "phase_b_subset.question_ids（30 题分层子集）",
      "command": "uv run python -m app.batch --ids <phase_b_subset> --no-memory",
      "semantics": "无跨 run 记忆读、无写回；memory_search 返回 enabled=false；meta.memoryArm=off",
      "role": "受控配对臂。只与 Phase A 中同题的 run 配对进 McNemar 2×2 表。"
    }
  },
  "controls": {
    "model": "qwen3.7-plus（LUUP_MODEL_ID 默认值），两臂同模型同端点",
    "harness_commit": "两臂共用同一个 commit；开跑前 harness 定版，跑完不改码。每个 run 的 exit.json.sourceIdentity{gitCommit,treeDirty} 自证，评估按 cohort 分组，dirty tree 单独成 cohort。",
    "question_bank": "backend/app/data/science125.json（retrievedAt 2026-08-08），两臂同题库同题干",
    "tool_implementations": "两臂同一份 tools/（arxiv_search / arxiv_save / paper_index_read / memory_search），唯一差异是 memory_dir 是否为 None",
    "turn_budget": {"scientist_max_turns": 22, "reviewer_max_turns": 19},
    "execution": "串行，一次一题，固定题号升序（百炼有速率限制；并发会把 429 混进失败分类）",
    "resumption": "已 exit 0 的题不重跑；断点续跑不改变题序",
    "batch_circuit_breakers": {
      "same_class_streak_stop": 5,
      "outage_streak_stop": 2,
      "outage_classes": ["infra_error", "infra_timeout"],
      "rationale": "批量的价值建立在「每题独立」上；连续同类失败正是该假设被证伪的信号，此时每多跑一题信息增益趋近 0 而成本不变。熔断触发即停批并记录，不得手动越过继续跑，否则本协议的 cohort 声明失效。"
    },
    "no_retry": "无隐式重试。契约不合格不重试是锁定契约；重试会把「一次做对」偷换成「多试几次总有一次对」。"
  },
  "phase_b_subset": {
    "fixed_before_phase_a": true,
    "note": "本子集在 Phase A 开跑前确定并落盘。事后按结果挑子集会把随机波动读成效应，这是本协议存在的首要理由。",
    "size": 30,
    "method": "deterministic_stratified_sampling",
    "seed": "luup-science125-phaseB-v1",
    "rule": [
      "1. 分层键 = science125.json 的 domain 字段，共 12 层，层大小合计 125。",
      "2. 比例分配：n_h = floor(N_h * 30 / 125)；若结果为 0 则抬到 1（保证每层至少一题，抬升过的层记为 raised）。",
      "3. 余额分配：k = 30 - sum(n_h)；在**未被抬升**的层里按 (N_h * 30 / 125) 的小数部分降序、同分按 domain 名升序，取前 k 层各 +1。raised 层已拿到超过比例份额，不参与二次分配。",
      "4. 层内挑选：把该层的题号按 sha256(f\"{seed}:{id}\") 的十六进制串升序排序，取前 n_h 个。",
      "5. 最终子集 = 各层所选题号并集，升序输出。"
    ],
    "allocation": {
      "Artificial Intelligence": {"stratum_size": 8, "allocated": 2, "ids": [120, 121]},
      "Astronomy": {"stratum_size": 23, "allocated": 5, "ids": [46, 49, 50, 55, 64]},
      "Biology": {"stratum_size": 22, "allocated": 5, "ids": [27, 28, 32, 35, 40]},
      "Chemistry": {"stratum_size": 9, "allocated": 2, "ids": [8, 12]},
      "Ecology": {"stratum_size": 8, "allocated": 2, "ids": [107, 110]},
      "Energy Science": {"stratum_size": 3, "allocated": 1, "ids": [117]},
      "Engineering & Materials Science": {"stratum_size": 4, "allocated": 1, "ids": [90]},
      "Information Science": {"stratum_size": 4, "allocated": 1, "ids": [91]},
      "Mathematical Sciences": {"stratum_size": 3, "allocated": 1, "ids": [1]},
      "Medicine & Health": {"stratum_size": 11, "allocated": 3, "ids": [16, 17, 19]},
      "Neuroscience": {"stratum_size": 12, "allocated": 3, "ids": [95, 100, 102]},
      "Physics": {"stratum_size": 18, "allocated": 4, "ids": [72, 77, 79, 86]}
    },
    "question_ids": [1, 8, 12, 16, 17, 19, 27, 28, 32, 35, 40, 46, 49, 50, 55, 64, 72, 77, 79, 86, 90, 91, 95, 100, 102, 107, 110, 117, 120, 121],
    "commitment": {
      "algorithm": "sha256",
      "payload": "question_ids 按升序、JSON 紧凑序列化（separators=(\",\",\":\")）、UTF-8 编码",
      "digest": "498002a5f4af6caac7036e6d0afa0565f0c0cce6f4dd7237ba22c81ece6ec9aa",
      "git_commit": "fe417584605716f71ef4e101c2ea4b55cbee19cf",
      "committed_at": "2026-08-13",
      "note": "承诺的是子集本身而不是本文件的自哈希——文件哈希写进文件是不动点，不可实现。子集的 digest 与生成规则由 backend/tests/test_experiment_protocol.py 逐条重算钉住，改题号必然红。"
    }
  },
  "metrics": {
    "mechanism": {
      "note": "机制指标只回答「消融是否真的发生、记忆是否真的被读到」。它们不是成绩，任何一条不成立都会让目标指标失去解释权。",
      "newInformationRate": "arxiv_search 中至少带回一篇本 run 未见论文的比例（statistics.searchHealth）",
      "deduplicatedRate": "同 run 缓存命中的检索占比（statistics.searchHealth）",
      "revisionRate": "review.json verdict==revise 占已评审 run 的比例（statistics.revision）",
      "memorySearchCalls": "全批 memory_search 调用数（statistics.memoryLeakage）",
      "memorySearchHits": "全批 memory_search 命中行数合计（statistics.memoryLeakage）；这是「125 题近似独立」的唯一证据——命中为 0 时，M4 才能理直气壮叫「解一道题的能力」，而不是「一道题带出下一道题」",
      "ablation_effectiveness_assertion": "memoryArm==off 的 run 必须没有任何一次真正读到记忆的 memory_search（事件里 enabled=true 或 hitCount>0 即为泄漏）。违反者标 ablationEffective=false 并**排除出配对**，在 statistics.memoryLeakage.ablationIneffectiveRuns 单列。依据：书 chapter10/generative-agents/validate_campaign.py 的 reflection_ablation_effective 门——被消融的机制必须交出零产出，否则该臂不是对照臂。"
    },
    "outcome": {
      "note": "目标指标才是要比较的东西，且只在机制指标全部成立时才允许解读。",
      "deliveryRate": "M4 交付率 ± 二项标准误 √(p(1-p)/n)；环境性失败单列一档（statistics.delivery）",
      "failureClassDistribution": "7 类失败分类的分布，按 3 桶（quality / infrastructure / unclassified）分组（statistics.failureClasses）",
      "passSquared": "M5 同题相邻两次 run 均交付的比例（statistics.passSquared）",
      "mcnemar": "memoryArms 配对的 b（off 失败∧on 通过）、c（off 通过∧on 失败）、discordant、concordantPass、concordantFail",
      "regressionRate": "c / (concordantPass + c)：在 off 已经能通过的题里，开记忆后反而挂掉的比例。分母为 0 时报 null 而不是 0。依据：hermes 消融引擎与 GAIA 的 negative_transfer_rate 都单列这个数——「没变差」比「变好」更容易站住。"
    }
  },
  "statistics": {
    "design": "bounded_comparison",
    "claims_allowed": ["方向（b 与 c 谁大）", "2×2 表的四个 cell 计数", "效应量（率差与二项标准误、regressionRate）"],
    "claims_forbidden": ["显著性主张", "p 值作为结论", "「记忆有效/无效」的普适断言", "从单条成功轨迹推广"],
    "reason": "n=30 配对时，不一致对（discordant = b + c）若 ≤5，即使 5:0 的极端一边倒，精确二项双侧 p 也只有 0.0625——**设计上就不可能显著**。此时报 p 值只会制造一个注定不显著的稻草人，或诱导事后加样本去追 p<0.05。因此本轮预先声明：只报方向、cell 计数与效应量，不做显著性主张。",
    "not_doing": {
      "seeds_and_repeats": "不加 seed / 重复采样 / bootstrap 基础设施。每题每臂各一次。成本为零、诚实度更高的替代是在报告里声明不做显著性主张，而不是造一套撑不起统计功效的重复设施。",
      "consequence": "run 内随机性与臂间差异不可分离，这一点写进 estimand，不靠事后免责。"
    },
    "reported_anyway": "evaluation.py 仍会输出 p 与 significant 字段（既有实现，不改），但本轮报告与 PPT 不得引用这两个字段。它们留在 JSON 里是为了将来样本足够时可以直接复用同一条链路。",
    "excluded_pairs": "ablationEffective=false 的 off run、缺题号的自由问题 run、只跑了一臂的题，都不进 2×2 表，单列计数。"
  },
  "declarations": {
    "note": "三条读数口径，与 phase_b_subset 同批在 Phase A 开跑前冻结。看到结果之后再补写的声明不成其为预注册，所以它们必须在这里、在任何 run 之前。",
    "pass_squared": "Pass^2 口径：passSquared 的「两次」是**同题时间相邻的两条既有 run**（evaluation.py 的 `_by_question` 按 run id 即 UTC 时间戳排序后取相邻对），属**机会样本**——两条 run 之间可能隔着 harness 改动、题库无关的时间漂移与不同的记忆状态。它**不是**设计的 k=2 独立重复采样，本轮也不做重复采样（见 statistics.not_doing）。报告与 PPT 引用 M5 时必须带上这句口径。依据：ai-agent-book chapter6 明文要求评估报告写清 k 次尝试的口径——是同一任务的 k 次独立采样，还是生产流水线上连续 k 个任务。",
    "verifier": "验证器口径：B1/B3/B4 由 `verify_offline_references` 纯离线判定，**B4 的作者真值取自本 run 落盘的论文卡片**（`memory/papers/`，即 agent 自己检索时存下的那份），不是验收时向 arXiv 实时反查——卡片错则 B4 一起错，这条通路不独立于 agent。**B2 是全部检查里唯一走网络的独立通路**（`ArxivClient.get_many` 逐条 resolve id 后比标题），因此引用真实性的独立证据面等于 B2 一条；B2 因 arXiv 不可达而失败时记 `infraError=true`，属环境性失败不属造假。`TITLE_OVERLAP_THRESHOLD=0.8`（`app/domain/references.py`）是**未经校准的自由参数**：它既没有标注语料也没有 ROC，其假阴/假阳率未知，B2 通过率的精度上界只能由后续人工抽检确定。在抽检出数之前，报告不得把 B2 通过率当作引用真实性的精确测量，只能当作下界筛子。",
    "failure_classes": "失败分类口径：**7 类 / 3 桶**，权威定义是 `backend/app/domain/runs.py` 的 `FailureClass`，3 桶归属（quality / infrastructure / unclassified）同样以该文件与 `evaluation.py` 的 `INFRASTRUCTURE_CLASSES` 为准，`agent_budget_exhausted` 属 quality 不属 infrastructure。7 类都有生成路径，但路径不同，报告须一并说明：`orchestrator.py` 的 `_classify` 只从异常映射出 4 类（reviewer_no_new_evidence / contract_violation / agent_budget_exhausted / infra_error），`verifier_refs` 与 `revision_no_change` 由 Harness 主流程直接判定，而 **`infra_timeout` 出自两条超时路径**：`app/services/launch.py` 的 `RunLauncher._wait`（HTTP 触发单 run，子进程超时后 kill，由父进程落盘）与 `app/batch.py` 的 `_run_one`（Phase A/B 的 `python -m app.batch` 单题超时后取消协程，由批次补写该 run 的 `exit.json` / `FAILED.md`）。两条路径共用同一个 `launch.RUN_TIMEOUT_SECONDS`（40 分钟），判据同源。因此本轮批跑里 `infra_timeout` **可以非零**：它读作「该题挂死、被判死并让批次继续」，属基础设施失败不属模型质量；连续 2 次即触发 outage 熔断停批（见 controls.batch_circuit_breakers）。**修订记录**：本条初稿（2026-08-13，registered_commit fe41758）声明「`infra_timeout` 只出自 HTTP 单 run 路径，批跑不经过它，故批跑 cohort 里计数恒为 0」。随后 `app.batch` 补上单题超时保护，那句路径事实即刻失效，本条于 **2026-08-13、Phase A 第一次开跑之前** 按新事实改写；改的是被声明的路径事实，不是读数口径或判据。"
  },
  "amendments": [
    {
      "date": "2026-08-14",
      "before_phase_a": true,
      "registered_commit_amended": "fe417584605716f71ef4e101c2ea4b55cbee19cf",
      "summary": "执行栈整体换成 TypeScript 栈。改的是本协议**被声明的事实**（谁在跑、run 落在哪、记忆走哪条通路），不是读数纪律。本条与 declarations.failure_classes 的修订记录同一形制：在 Phase A 第一次开跑之前、看到任何结果之前落盘。",
      "changes": {
        "execution_stack": "执行栈由 Python 两角色（Scientist / Reviewer，`app/agent/orchestrator.py`）换为 TypeScript 五角色固定流水线（researcher / hypothesis-generation / evidence-review / research-plan / reviewer，`src/harness.ts`）：luup-old 的 TS 栈复活为迁移基底，再叠加 B1–B4 确定性引用验收（`src/verify/`）。两条上界不变——补证至多 2 轮、修订至多 2 轮。开跑命令随之改为 `pnpm batch --ids 1-125`（Phase A）与 `pnpm batch --ids <phase_b_subset> --no-memory`（Phase B），取代 arms 里记的 `uv run python -m app.batch ...`。turn budget 的形态也变了：不再是 scientist 22 / reviewer 19 的两角色预算，而是每阶段一个 SDK maxTurns 加一个阶段级 deadline。B1–B4 的判据本身不变，只有 B2/B4 的真值来源随数据形状改写（见 `src/verify/references.ts` 顶部），declarations.verifier 关于 `TITLE_OVERLAP_THRESHOLD=0.8` 是未经校准自由参数的声明**继续有效**，阈值一字未改。",
        "run_storage": "run 存储由 `runs/<ts>/` 文件树换为 `node:sqlite` 单库（`src/store/`）。`runs/` 降为**只读归档**：Python 期跑批留下的证据继续可读、不再被写入，也不再是评估的输入。离线评估随之从读工件目录（`app/evaluation.py`）改为只读打开数据库（`src/eval/metrics.ts`）。cohort 分组的依据不变——每个 run 仍自证 `sourceIdentity{gitCommit,treeDirty}`，dirty tree 仍单独成 cohort。",
        "memory_channel": "记忆通道由「确定性注入 + `memory_search` 工具」**收窄为「仅确定性注入」**。TS 栈没有 `memory_search`，模型没有任何自主读记忆的通路：只有批跑发起 run 之前确定性读同题页末 ≤3 行、注入 researcher 输入这一条。三处口径随之改写：(1) 消融臂语义由「工具返回 enabled=false」变为**注入开 / 关**，`--no-memory` 同时关掉注入与写回；(2) estimand 第 3 条「tool visibility differs：两臂工具面相同而工具效用不同，agent 仍可能把一次空调用花在它上面」**就此失效**——两臂工具面现在完全相同，被消融的是数据通道本身，agent 没有可浪费的空调用；(3) 泄漏度量的口径由 memory_search 事件计数改为**注入事件计数**：每个 run 落一条 `campaign.prior_attempts{question_id,count}`，metrics.mechanism 的 `memorySearchCalls` / `memorySearchHits` 由 `memoryInjection.entries` 及其分臂拆分取代，`ablation_effectiveness_assertion` 相应读作「memory_arm=off 的 run，`campaign.prior_attempts.count` 必须为 0；大于 0 即消融失效，标 ablationEffective=false 并**排除出配对**」。判据的形状没变（被消融的机制必须交出零产出），只有被数的东西变了。战役记忆本身仍是 repo 根 `memory/` 的 append-only Markdown、确定性字符匹配、零 embedding——存储没跟着 run 数据搬进 SQLite，历史战役页因此连续可读。",
        "corrections": "corrections 机制存在且被记账：一个业务 Attempt 内至多一次结构化纠错（模型输出不满足领域合同时把违规项交还重出），记在 `attempts.corrections` 上，**不虚增 Attempt 计数**。它不是重试——controls.no_retry 的「无隐式 Attempt 重试」**原样有效**：契约不合格不会重新发起一个 Attempt，纠错仍不过即终止。Python 期没有这个字段，故本轮新增 `statistics.corrections` 一项过程指标（分母是 Attempt 不是 run），它属机制指标，不参与目标指标的解读。"
      },
      "unchanged": "读数纪律一条不动：statistics.design 仍是 `bounded_comparison`，仍不做显著性主张、不引用 p 与 significant、不加 seed / 重复采样；phase_b_subset 的 30 题、种子 `luup-science125-phaseB-v1`、分层规则与 sha256 承诺 `498002a5...` **一字未改**，仍由重算测试逐条钉住（Python `backend/tests/test_experiment_protocol.py`，TS `test/experiment-protocol.test.ts`）；declarations 的三条口径（pass_squared 的机会样本、verifier 的未校准阈值、failure_classes 的 7 类 / 3 桶与 infra_timeout 可非零）继续有效，只是权威文件路径随执行栈改为 `src/agent/failures.ts` 与 `src/eval/metrics.ts` 的 `INFRASTRUCTURE_CLASSES`。interpretation 的结论形状不变。"
    }
  ],
  "interpretation": "这是对已记录架构取舍的证据，不是普适优越性主张。结论的形状只能是「在这一条 harness commit、这一个题库、这一条串行顺序下，跨 run 记忆让交付率朝某个方向动了多少」，不能读成「记忆机制优于无记忆」或反过来。Do not claim superiority from one successful trace, one direction, or an unpriced token count.",
  "provenance": {
    "method_source": "ai-agent-book chapter8 / chapter10",
    "protocol_shape": "chapter10/multi-role-transfer/experiment_protocol.json",
    "ablation_gate": "chapter10/generative-agents/validate_campaign.py 的 reflection_ablation_effective",
    "metric_split": "chapter10/multi-role-transfer/README.md 的机制指标 / 目标指标分法",
    "repo_anchors": ["docs/design/criteria.md H 节", "docs/design/loop-upgrade.md D1-D3"]
  }
}
