# 百炼 OpenAI-compatible Responses 接线；不要在此文件写真实密钥。
#
# 两种用法，任选其一（也可混用）：
#   1. `cp .env.example .env` 后填值——仓根的 .env 会被自动读取，无需 export，
#      从 backend/ 起 uvicorn 或跑 CLI 都找得到（路径按仓根解析，与 cwd 无关）。
#   2. 直接 export 同名系统环境变量。
# 同名时**系统环境变量优先**，.env 只是兜底；.env 不存在也能正常启动（缺凭据才报错）。
QWEN_API_KEY=your-key
QWEN_BASE_URL=your-openai-compatible-base-url
LUUP_MODEL_ID=qwen3.7-plus
