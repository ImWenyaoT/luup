# questions/

每题一页：`q<id>.md`（id = Science-125 题号）。首次 `memory_note` / run 收尾时由代码创建。

页内 append-only，记录：本题跑过几次、每次 verdict、成功 run 的目录指针、**跨 run 被拒的假设与理由**、有效检索词、领域文献覆盖评估。

重派 hypothesis 节点时，本页的跨 run 负结果会与本 run 的 `memory/rejected.md` 一起打进 message —— 这是防止跨 run 反复端上同一条死路的唯一凭据。
