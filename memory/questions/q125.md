# q125

Science-125 第 125 题的跨 run 战役页。**append-only**：新记录追加在文末，
旧记录不改写、不删除（覆盖会让已经走死的路重新变得可走）。
由 `src/campaign/campaign.ts` 在 run 收尾时确定性追加；模型没有写本目录的通路。
（本页的历史记录由 Python 期的同名模块写出，ADR-0004 后由 TS 侧接手，行格式一致。）

记录内容：状态与 verdict、成功 run 的目录指针、跨 run 被拒假设与理由、有效检索词、领域文献覆盖评估。

## [2026-08-08T13:40:14.431Z] run.ts

- verdict: ALL PASS
- run: /home/ail510/tian_wenyao/projects/luup/runs/20260808-071315

回填自历史 run（rebuild-memory.ts）；工件与 verdicts 见 run 目录。
