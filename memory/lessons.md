# 运营级教训

append-only：新条目追加在文末，旧条目不改写、不删除。由 `memory_note(target="lessons")` 写入。

记什么：跨题复用的运营判断 —— 哪些学科 arXiv 覆盖差、哪类检索词有效/无效、哪种假设方向在多题上反复被拒。
不记什么：单题结论（那属于 `questions/q<id>.md`）、文献元数据（那属于 `library/`）。

样本少的统计性断言请在正文里写明样本量（「跑了 3 题」不等于结论）。
